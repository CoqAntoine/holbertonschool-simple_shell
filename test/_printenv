#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <sys/types.h>
#include <string.h>

extern char **environ;

int main(int ac, char *av[], char **env)
{
	printf("%d, %s\n", ac, av[0]);

	printf("env : %s\n", **&env);
	printf("environ : %s\n", **&environ);

	return (0);
}
